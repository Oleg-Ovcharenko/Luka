function initMap() {
  var map = new google.maps.Map(document.getElementById('map'), {
    zoom: 18,
    center: {
      lat: 49.8337136,
      lng: 24.0125895
    },
    disableDefaultUI: true
  });
}
